# SchemaLock for Zed

Schema-pinned validation, completion, and hover for Kubernetes-style YAML in the
[Zed](https://zed.dev) editor, backed by the SchemaLock CDN.

> Documentation in progress — see `RELEASING.md` for publishing.
