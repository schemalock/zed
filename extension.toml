id = "schemalock"
name = "SchemaLock"
description = "Schema-pinned validation, completion, and hover for Kubernetes-style YAML, backed by the SchemaLock CDN."
version = "0.1.0"
schema_version = 1
authors = ["SchemaLock"]
repository = "https://github.com/schemalock/zed"

[language_servers.schemalock]
name = "SchemaLock"
languages = ["YAML"]
